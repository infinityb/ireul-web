<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>


<% if @songs %>
	<ul>
	<% @songs.each do |song| %>
		<li><%= song.title %></li>
	<% end %>
	</ul>
<% end %>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
