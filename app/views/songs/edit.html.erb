<div class="container">
  <div class="header">
    <h1>Edit song</h1>
    <%= link_to 'Show', @song %>
    <%= link_to 'Back', songs_path %>
  </div>

  <%= render "metadata_section" %>

  <div class="header">
    <h2>Audio</h2>
  </div>
  <div>
    <% if @song.file %>
      <audio src=<%= @song.file.url %> controls></audio>
    <% end %>
  </div>

  <%= render 'form_file_only' %>

  <%= render "images_section_small" %>
</div>
