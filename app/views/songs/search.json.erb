<% songs = @songs.map do |s|
  {
    id: s.id,
    artist: s.artist,
    title: s.title,
  }
end

res = {
  status: :ok,
  query: @query,
  time: Time.now.utc,
  results: songs
} %>

<%= res.to_json.html_safe %>
